#include<stdio.h>
int main(){

    // A string in C is just a group of characters (like letters or symbols)
    //  put together, ending with a special marker called '\0' that tells 
    // the computer “this is the end of the word.”

    //                          or

    //In C programming, a string is a sequence of characters stored in a 
    // contiguous block of memory, terminated by the null character '\0'.


}
#include<stdio.h>
int main(){
    int i = 10;
    float f = 3.342;
    char c = '#';

    //shift + alt + bottom arrow 
    printf("integer i contains : %d, size of int: %d\n",i,sizeof(i));
    printf("integer f contains : %f, size of float: %d\n",f,sizeof(f));
    printf("integer c contains : %c, size of char: %d\n",c,sizeof(c));

    //1 byte = 8 bites
}